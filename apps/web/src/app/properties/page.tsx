export default function PropertiesPage() {
  return (
    <main className="shell">
      <h1>Properties</h1>
      <p>Connect this page to `GET /api/properties` and render searchable listings.</p>
    </main>
  );
}
